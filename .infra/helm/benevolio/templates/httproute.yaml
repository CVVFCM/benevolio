apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
    name: {{ include "benevolio.fullname" . }}
    labels:
        {{- include "benevolio.labels" . | nindent 8 }}
spec:
    parentRefs:
        -   name: main
            namespace: traefik
            sectionName: websecure
    hostnames:
        - {{ .Values.host | quote }}
    rules:
        -   matches:
                -   path:
                        type: PathPrefix
                        value: /
            backendRefs:
                -   name: {{ include "benevolio.fullname" . }}-web
                    port: 80
